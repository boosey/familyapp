# Handoff — ADR-0014 Increment 4 (intake unification) + Increment 5 (observability / doc-true / ADR close)

You are picking up the **final two increments** of ADR-0014 ("composing surface"). **Inc 3 is COMPLETE**
(slices 1–11, all green + cold-reviewed). Your job: Inc 4, then Inc 5, on the existing branch. **Do not
push, do not merge to master, do not deploy** — leave the finished branch for a human go/no-go.

## The authoritative specs — READ FIRST, do not duplicate (they supersede this handoff on conflict)
- **Rollout roadmap** `docs/superpowers/plans/2026-07-03-composing-surface-adr0014-rollout.md` — **§Increment 4**
  and **§Increment 5 are your spec.** This handoff only adds the deltas the docs don't yet reflect.
- **AFK master handoff** `docs/superpowers/plans/2026-07-04-adr0014-remainder-afk-handoff.md` — §"Increment 4",
  §"Increment 5", the AFK operating rules, the environment block (worktree path, commit-author gate, commands),
  and the stop conditions. **All still apply verbatim** — reuse them; don't restate.
- **Inc 3 blueprint** `docs/superpowers/plans/2026-07-03-adr0014-inc3-blueprint.md` — Build-status is now current
  **through Slice 11 (Inc 3 complete)**; RESOLVED DECISION (b) is the intake-reuse mandate for Inc 4.
- **Frozen shared contract** `docs/superpowers/plans/2026-07-03-adr0014-shared-contract.md` — §4 core signatures,
  §6 orchestration. **Do NOT amend in an increment; STOP if you seem to need to** (a schema change or a core
  write-path signature change is an AFK stop-condition).
- **Persistent memory** `project_adr0014_rollout_status.md` — current through Slice 11. Update after each unit.
- Root `CLAUDE.md` — single-front-door rule, vendor-seam rule, subagent-driven workflow, commit-author gate.

## Environment (unchanged from the AFK master handoff)
- Worktree `C:/Users/boose/projects/familyapp/.claude/worktrees/composing-surface-inc1-3`, branch
  `worktree-composing-surface-inc1-3`, **HEAD `6e72581`** (Inc 3 complete). Prefix every shell command with an
  absolute `cd "…/composing-surface-inc1-3" &&`. Author every commit
  `--author="Alex Boudreaux <boosey.boudreaux@gmail.com>"` (Vercel gate).
- Per-commit bar = `pnpm -r test` + `pnpm -r typecheck` GREEN. Windows; the full web suite ~2 min (run it in the
  background and wait for the completion notification — piped output only flushes at the end).
- Verified baselines at HEAD: core 277, pipeline 74, capture 38, db 67, **apps/web 403**, typecheck 0.

## What Inc 3 left you (the reuse points — this is the part the docs don't yet capture)
- **`<ComposingEditor>` exists** at `apps/web/app/hub/ComposingEditor.tsx`. `StoryComposer.tsx` is now a thin
  answer/tell chrome wrapper that mounts it. **Current props:** `{ ask?, draft, backTab, resumeHref? }` — it does
  the full composing machine (3 phases keyed on story state: no-draft capture / `draft` live editor+footer+Finish /
  `pending_approval` review). **IT HAS NO `mode`/`kind` PROP YET.** Decision (b) says intake mounts
  `<ComposingEditor mode="intake">`; I deliberately did NOT add that axis in slice 10 (YAGNI — no intake caller
  existed). **Inc 4 adds it:** thread a `mode: "story" | "intake"` prop and branch the terminal behavior — for
  intake, Save stops at anchor extraction and there is NO Share/tier/consent step (per roadmap §Inc 4), whereas
  story finishes → `pending_approval` → Share. Everything else (append/Cleanup/✨Polish/Finish-check/undo/redo, the
  unified in-flight mutation lock) is shared and should be reused as-is.
- **The mutation lock** in `ComposingEditor` is `busy` + `otherMutationInFlight` (covers record/append/decline/
  finish/polish). If you add intake-specific mutations, fold them into those flags the same way — do NOT invent a
  parallel guard. The cold reviews hammered this; keep it exhaustive.
- **Intake target files:** `apps/web/app/hub/about-you/AboutYouFlow.tsx` + its `actions.ts` (colocated), tests in
  `apps/web/__tests__/about-you-flow.test.tsx`. Confirm `augmentProfileFromStory` fires post-approval for STORIES
  (it does, in `shareAnswerAction`) and at SAVE for INTAKE. Wire the **consent-gated narrator-memory extraction
  seam** as a real call-site — a **stub/no-op is acceptable** per the roadmap (the extraction model is deferred).
  Intake audio+transcript are RETAINED (see memory `intake_audio_retained`); intake is still NOT a Story.
- **Post-Slice-10/11 copy that is now dead** (Inc 5 retires it): `hub.answer.finishing`, `hub.answer.takingLonger`,
  and the "Polishing your words" strings `hub.answer.polishing`/`polishingSub` are only reachable via the take-0
  `AnswerReviewPending` in-flight screen now — check reachability before deleting.

## Increment 5 specifics the docs list but that need pointers
- Verbose logging: server via `plog` (already used across `answer/[askId]/actions.ts`); add client `[chronicle]`
  logs for the capture-state transitions in `ComposingEditor`. Confirm `CHRONICLE_PIPELINE_LOG=1` surfaces the
  record→cleanup→append→finish sequence.
- Rewrite `docs/Recording-To-Story-Pipeline.md` to the new authored-prose flow (the monolithic render is gone).
- Set `docs/adr/0014-*` Status → Implemented (+ implementation notes); note the ADR-0007 "canonical = original
  record" amendment; update `docs/PLAN.md` / `docs/PROGRESS.md`; retire the "Polishing your words" copy.
- **`docs/undone 7-4.md` lives at the REPO ROOT** (`C:/Users/boose/projects/familyapp/docs/undone 7-4.md`), NOT in
  the worktree — it is untracked on the main checkout. Its Tier-1 item #1 says ADR-0014 is "0% reachable by any
  user on master"; correct it to "complete on the branch, pending human merge to master." **Do NOT delete the file.**

## Carried-over DEFERRED item (do NOT silently drop it)
Core `logPolish` (`packages/core/src/story-repository.ts`) writes `stories.prose` with **no priorProse/staleness
guard**, unlike the append/finish paths. The slice-10 client mutation lock closes the single-client UI-reachable
clobber, so this only matters for concurrent multi-session writes. Adding a guard is a **frozen-contract §4 signature
change** → an AFK stop-condition, so it was deferred. If Inc 4/5 doesn't touch it, carry it forward as a hardening
follow-up (it is NOT a blocker for the branch).

## Method (repo-mandated) + AFK reliability note
Subagent-driven TDD, ONE writer in the worktree at a time; a fresh **cold** adversarial reviewer per unit reviewing
the **immutable commit**; iterate until clean with a NEW cold reviewer each round. **VERIFY EVERYTHING YOURSELF**
after any builder (`git show --stat`, `pnpm -r typecheck`, affected suites AND the full `apps/web` suite) before
trusting a count — the named builder sub-agents in this repo go idle without a prose report and over-claim green.
The slice-10 run did orchestrator-writes-code + fresh-cold-review-of-the-commit and it caught 5 real bugs across 4
rounds (incl. a deterministic data-loss bug); keep the fresh-cold-review-of-the-commit step no matter which
build/write split you choose. Companion regression test after every bug fix.

## Stop conditions (halt, write a status note, don't thrash)
A unit can't go green after two builder+review rounds; a change would require amending the frozen shared contract;
a **schema change** is needed (reseeds both Neon branches — OUT of AFK scope); or the architecture guard
(`packages/core/test/architecture.test.ts`) fails and the only fix is a new content-path allowlist entry you're
unsure is legitimate. Commit what's green, record the blocker in the blueprint Build-status + memory, stop.

## When you finish (or hit a stop condition)
Update `project_adr0014_rollout_status.md` + the blueprint Build-status + `MEMORY.md` after EACH unit (SHA,
baselines, what landed, next). At the end, write a completion note listing the human checkpoints: merge-to-master
go/no-go, Neon reseed IF any schema moved (none should — Inc 4/5 are web + docs), the deferred `logPolish` guard, and
the Inc-5 human sign-offs (ADR status flip, doc rewrite review). Leave the branch committed and green; do NOT merge.

## Suggested skills for the next session
- `superpowers:subagent-driven-development` (or `superpowers:executing-plans`) — the builder/reviewer loop.
- `superpowers:test-driven-development` — each unit is TDD (red→green).
- `superpowers:requesting-code-review` / `superpowers:receiving-code-review` — the fresh-cold-review discipline
  (verify findings before implementing; slice 10 rejected a reviewer's exact fix location that would have read a
  stale value, and fixed it correctly elsewhere).
- `frontend-design:frontend-design` — Inc 4 is a real intake UX using the shared editor (Kindred design system; see
  memory `reference_ui_design_system` / `project_design_handoff`) if you touch visual structure.
