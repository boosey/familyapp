# Handoff — ADR-0014 Increment 5 (observability + doc-truing + ADR close)

You are picking up the **final increment** of ADR-0014 ("the composing surface"). **Inc 0–4 are COMPLETE and green**;
Inc 4 also **merged master into the branch** and put its schema change on the migration infra. Your job: Inc 5 —
observability, doc-truing, and closing the ADR. **Do not push, do not merge to master, do not deploy** — leave the
finished branch for a human go/no-go.

## Environment (verify before you start)
- Worktree `C:/Users/boose/projects/familyapp/.claude/worktrees/composing-surface-inc1-3`, branch
  `worktree-composing-surface-inc1-3`, **HEAD `70aefb8`**. Prefix every shell command with an absolute
  `cd "…/composing-surface-inc1-3" &&`. Author every commit `--author="Alex Boudreaux <boosey.boudreaux@gmail.com>"`
  (Vercel git-author gate — deploys are blocked otherwise).
- **Green baselines at HEAD** (verified 2026-07-04, post master-merge): apps/web **467**, db **82** (incl.
  migration-drift), all 13 packages `pnpm -r test` exit 0, `pnpm -r typecheck` clean. Windows; the full web suite
  ~1–2 min (run it and wait — piped output flushes at the end).
- **The branch now contains master's features** (imagery Phase 2–4, ADR-0006, deletion/erasure) + the drizzle
  **migration chain** (`0000_baseline → 0001_soft_giant_girl → 0002_sudden_rhino` = intake_revisions). `db:generate`
  emits snapshot + a migration; invariant (trigger) changes are HAND-CARRIED into the emitted migration. Do NOT run a
  destructive reseed — schema ships additively via `db:migrate` at deploy.

## Authoritative specs — READ FIRST (they supersede this handoff on conflict)
- **Rollout roadmap** `docs/superpowers/plans/2026-07-03-composing-surface-adr0014-rollout.md` — **§Increment 5 is your
  spec.**
- **ADR** `docs/adr/0014-composing-surface-authored-prose-and-the-four-passes.md` — §Consequences "Observability" +
  the Status line you will flip. Its §8 already has an Inc-4 implementation note.
- **Inc 4 blueprint** `docs/superpowers/plans/2026-07-04-adr0014-inc4-blueprint.md` — the BUILD STATUS section (what
  landed, the two documented deferrals) is current.
- **Persistent memory** `project_adr0014_rollout_status.md` — current through the Inc-4 master merge. Update it after.
- Root `CLAUDE.md` — single-front-door rule, vendor-seam rule, subagent-driven workflow, commit-author gate.

## Inc 5 work items

### 1. Observability (the only real CODE work — the original live test had NO logs; `plog` is hard-off in prod)
- **Server**: `plog`/`plogError` are already used across `apps/web/app/hub/answer/[askId]/actions.ts` and
  `apps/web/app/hub/about-you/actions.ts`. Audit that the full record → type → cleanup → append → polish → finish →
  share (story) and record → cleanup → polish → save (intake) sequences each emit a coherent, correlated log line at
  every stage. Add any missing ones (e.g. intake `submitIntakeRecording`/`polishIntakeAnswerAction`/`saveIntakeAnswer`).
- **Client**: add `[chronicle]` `console` logging for the CAPTURE-STATE TRANSITIONS in `ComposingEditor.tsx`
  (record start/stop, take appended, polish tap, finish probe/accept/decline, review→share) — the live test lacked
  exactly this "is it slow or stuck?" signal. Consider a tiny `clog()` helper gated on a client flag so it's quiet by
  default. Keep it out of the hot render path.
- Confirm the prod toggle **`CHRONICLE_PIPELINE_LOG=1`** actually surfaces the whole server sequence (grep the plog
  implementation for the env gate; document how to turn it on in prod).
- Tests: assert the key stages log (spy on plog) if cheap; at minimum don't regress. Companion regression test after
  any bug fix (repo rule).

### 2. Doc-truing
- `docs/Recording-To-Story-Pipeline.md` — REWRITE to the new authored-prose flow (per-take Cleanup at record +
  metadata derivation at Finish; the monolithic render-and-transition-on-stop is gone). Remove any "current shipped"
  banner.
- `docs/adr/0014-*` — **Status → Implemented** (+ short implementation notes; §8 note already exists). `docs/adr/0007-*`
  — add the "canonical = un-discardable original record, NOT regenerable source" amendment note (ADR-0014 §7).
- `docs/PLAN.md` / `docs/PROGRESS.md` — mark ADR-0014 Inc 0–4 done + Inc 5 done.

### 3. Retire the dead "Polishing your words" copy (`apps/web/app/_copy/hub.ts`)
- Candidates now dead after slice 10/11 phase-collapse: `hub.answer.polishing` / `polishingSub`, `hub.answer.finishing`,
  `hub.answer.takingLonger`. **CHECK REACHABILITY before deleting** — some may still be reached via the take-0
  `AnswerReviewPending` in-flight screen (`apps/web/app/hub/answer/[askId]/AnswerReviewPending.tsx`) and the live
  link-session surface. Grep each key across `apps/web`; delete only the truly-unreferenced ones; keep the rest.

## Method (repo-mandated)
Subagent-driven TDD, ONE writer in the worktree at a time; a FRESH cold adversarial reviewer per unit reviewing the
IMMUTABLE commit; iterate until clean with a NEW cold reviewer each round. **VERIFY EVERYTHING YOURSELF** after any
builder (`git show --stat`, `pnpm -r typecheck`, affected suites AND the full `apps/web` suite) before trusting a
count — builders in this repo go idle without a prose report and over-claim green. Commit author = boosey. Do NOT
push/merge/deploy.

## Carry-over DEFERRED items (record; do NOT silently drop)
1. **`logIntakePolish` origin-keying edge** (`packages/core/src/intake-answer-repository.ts`): it picks
   `user_authored` vs `human_corrected` from `priorRevs.length === 0`, not `intake_answers.origin`. If
   `submitIntakeRecording`'s best-effort `ai_cleaned` ledger write silently fails while the `text` write succeeds, a
   later Polish mislabels AI-origin text as `user_authored`. Deep edge (already-degraded ledger); fix = key off
   `origin === "typed"`. Hardening follow-up, not a blocker.
2. **Core `logPolish` staleness guard** (`packages/core/src/story-repository.ts`): no priorProse/staleness guard,
   unlike the append/finish paths. The client mutation lock closes the single-client UI-reachable clobber; only matters
   for concurrent multi-session writes. Adding a guard is a frozen-contract §4 signature change — deferred.
3. **S4 redundant read**: `shareAnswerAction` does a second `getStoryForViewer` for the memory feed (scoping; nit).

## Stop conditions
A unit can't go green after two builder+review rounds; a change would require a frozen shared-contract §4 signature
change or a schema change; the architecture guard (`packages/core/test/architecture.test.ts`) fails and the only fix
is a new content-path allowlist entry you're unsure is legitimate. Commit what's green, record the blocker in the
blueprint + memory, stop.

## When you finish
Update `project_adr0014_rollout_status.md` + `MEMORY.md` + the blueprint BUILD STATUS. Write a completion note listing
the human checkpoints: merge-to-master go/no-go (which triggers the Neon `db:migrate` for `0002` at the Vercel build),
the deferred hardening items, and the ADR status-flip sign-off. Leave the branch committed and green; do NOT merge.

## Suggested skills
- `superpowers:subagent-driven-development` / `superpowers:executing-plans` — the builder/reviewer loop.
- `superpowers:test-driven-development` — each unit is TDD.
- `superpowers:requesting-code-review` / `superpowers:receiving-code-review` — verify findings before implementing.
