# Google Drive's filesystem does not support the symlinks pnpm's default store layout needs.
# The hoisted linker produces a flat node_modules (no internal symlinks), which works on the
# cloud-synced drive. See docs/OPEN-QUESTIONS.md — moving the repo off Drive is recommended.
node-linker=hoisted
symlink=false
shamefully-hoist=true
